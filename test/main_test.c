#include <unity.h>

void setUp(void) { // required only on Windows
    // set stuff up here
}

void tearDown(void) { // required only on Windows
    // clean stuff up here
}

int main( int argc, char **argv) {
    UNITY_BEGIN();
    UNITY_END();
}